{
  "name": "use-doc-title",
  "version": "0.1.0",
  "description": "A pretty custom hooks for document title and react",
  "license": "MIT",
  "main": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "scripts": {
    "compile": "tsc ./src/index.ts --jsx react --declaration --outDir ./dist",
    "clean": "rimraf dist",
    "build": "npm run clean && npm run compile",
    "publisher": "npm run build && npm publish"
  },
  "homepage": "https://github.com/Danue1/use-doc-title",
  "repository": "Danue1/use-doc-title",
  "bugs": "https://github.com/Danue1/use-doc-title/issues",
  "keywords": [
    "react",
    "react-dom",
    "react-hooks",
    "hooks",
    "custom hooks",
    "title",
    "document-title",
    "react-css",
    "react-document-title",
    "custom"
  ],
  "devDependencies": {
    "@types/react": "^16.8.8",
    "rimraf": "^2.6.3",
    "typescript": "^3.3.3333"
  }
}
